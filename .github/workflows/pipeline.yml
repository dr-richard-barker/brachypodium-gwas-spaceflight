name: AstroGrass Pipeline & GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas numpy scipy matplotlib seaborn statsmodels python-docx reportlab tqdm

      - name: Execute AstroGrass Automated Pipeline
        run: |
          python code/run_pipeline.py --skip-download

      - name: Verify Build Artifacts
        run: |
          test -f tables/astrograss_master_table.csv
          test -f tables/video_catalog.csv
          test -f figures/fig1_gravitropic_kinetics.png
          test -f figures/fig2_osd375_sample_matrix.png
          test -f figures/fig3_candidate_pathways.png
          test -f figures/fig4_cross_species_conservation.png
          test -f figures/fig5_mechanistic_model.png
          test -f figures/fig6_astrograss_architecture.png
          test -f figures/figS1_mission_experimental_design.png
          test -f manuscript/manuscript.pdf
          test -f manuscript/manuscript.docx
          echo "✓ All core database, figure, and manuscript deliverables verified."

      - name: Setup GitHub Pages
        if: github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v4

      - name: Upload Pages Artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'docs/'

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build-and-test
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
